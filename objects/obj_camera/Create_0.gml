/// @desc Camera set up
cam = view_camera[0];
toFollow = obj_ooze;
view_w = camera_get_view_width(cam) * 0.5;
view_h = camera_get_view_height(cam) * 0.5;

tarx = toFollow.x;
tary = toFollow.y;

xTo = xstart;
yTo = ystart;


